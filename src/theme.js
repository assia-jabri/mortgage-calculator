import { createTheme } from '@mui/material/styles';


export const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  
});


// In this file we can costumize all our MUI components 
// To use them in a way That matches UI rules